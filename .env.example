# Backend root URL (no trailing slash). Requests go to <this>/api/v1/... (e.g. /api/v1/wallet-auth/nonce).
# Copy this file to .env and set VITE_API_BASE_URL for your environment.
#
# Local dev (frontend on localhost:5173, backend on Railway):
#   VITE_API_BASE_URL=https://digimaaya-api-production.up.railway.app
#   (Backend must allow CORS origin http://localhost:5173 – add to CORS_ORIGIN on Railway.)
#
# Production (Vercel): set VITE_API_BASE_URL in Vercel env vars; same backend URL.
#
VITE_API_BASE_URL=https://digimaaya-api-production.up.railway.app

# Backend .env: set FRONTEND_URL to the frontend origin for Google OAuth callback.
#   Local:   http://localhost:5173
#   Deploy:  https://digimaya-launchpad-front.vercel.app
